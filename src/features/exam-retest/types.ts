export type RetestRequest = {
  id: string;
  name: string;
  badgeColor: string;
  statusLabel: string;
  statusColor: string;
  classLabel: string;
  proposedAt: string;
  subject: string;
  preferredTime: string;
  reason: string;
  location: string;
  teacher: string;
};
